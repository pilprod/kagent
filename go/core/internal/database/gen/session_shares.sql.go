// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: session_shares.sql

package dbgen

import (
	"context"
)

const createSessionShare = `-- name: CreateSessionShare :one
INSERT INTO session_share (token, session_id, user_id, read_only)
VALUES ($1, $2, $3, $4)
RETURNING id, token, session_id, user_id, read_only, created_at
`

type CreateSessionShareParams struct {
	Token     string
	SessionID string
	UserID    string
	ReadOnly  bool
}

func (q *Queries) CreateSessionShare(ctx context.Context, arg CreateSessionShareParams) (SessionShare, error) {
	row := q.db.QueryRow(ctx, createSessionShare,
		arg.Token,
		arg.SessionID,
		arg.UserID,
		arg.ReadOnly,
	)
	var i SessionShare
	err := row.Scan(
		&i.ID,
		&i.Token,
		&i.SessionID,
		&i.UserID,
		&i.ReadOnly,
		&i.CreatedAt,
	)
	return i, err
}

const deleteSessionShare = `-- name: DeleteSessionShare :exec
DELETE FROM session_share
WHERE token = $1 AND session_id = $2 AND user_id = $3
`

type DeleteSessionShareParams struct {
	Token     string
	SessionID string
	UserID    string
}

func (q *Queries) DeleteSessionShare(ctx context.Context, arg DeleteSessionShareParams) error {
	_, err := q.db.Exec(ctx, deleteSessionShare, arg.Token, arg.SessionID, arg.UserID)
	return err
}

const getSessionShareByToken = `-- name: GetSessionShareByToken :one
SELECT id, token, session_id, user_id, read_only, created_at FROM session_share
WHERE token = $1
LIMIT 1
`

func (q *Queries) GetSessionShareByToken(ctx context.Context, token string) (SessionShare, error) {
	row := q.db.QueryRow(ctx, getSessionShareByToken, token)
	var i SessionShare
	err := row.Scan(
		&i.ID,
		&i.Token,
		&i.SessionID,
		&i.UserID,
		&i.ReadOnly,
		&i.CreatedAt,
	)
	return i, err
}

const listSessionSharesBySession = `-- name: ListSessionSharesBySession :many
SELECT id, token, session_id, user_id, read_only, created_at FROM session_share
WHERE session_id = $1
ORDER BY created_at DESC
`

func (q *Queries) ListSessionSharesBySession(ctx context.Context, sessionID string) ([]SessionShare, error) {
	rows, err := q.db.Query(ctx, listSessionSharesBySession, sessionID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SessionShare
	for rows.Next() {
		var i SessionShare
		if err := rows.Scan(
			&i.ID,
			&i.Token,
			&i.SessionID,
			&i.UserID,
			&i.ReadOnly,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
